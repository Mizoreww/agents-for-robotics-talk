# v0.18 P3/P23 vertical-position follow-up — 2026-09-16

Removed the requested P3 tagline and the P23 Evidence / Still open blocks.
Restored the original horizontal positions, widths, alignment and title positions.
Only body vertical positions change; the requested text remains removed.
Fonts, columns, diagrams and all other slides are preserved.
Chinese script is unchanged and still matches the embedded script.

Verification: 22 tests passed; current-hash static, full layout, playback/pause/restart,
figure controls, navigation, notes and mobile UI passed. P3/P23 inspected visually.
All other slides and all media metadata are exact baseline matches; static validation
also checks the actual embedded media bytes. Earlier uninterrupted clip playback and
Standards/Spec review remain evidence for the unmodified content, not a new review.

Current authority: `.build/v0_18_centering_manifest.json`.

Latest follow-up: removed only the P23 Chapter overview subtitle. Exact comparison against `.build/v0_18_p23_subtitle_before.json` proves every other slide element, position, script and media record is unchanged.
